# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Streamlit secrets（絶対にコミットしない）
.streamlit/secrets.toml

# ローカルデータファイル（個人データ・ポジション情報）
turtle_save.json
master_save.pkl

# X収集DB（ローカルのみ・Streamlit Cloud では再生成）
x_tweets.db
twscrape_accounts.db

# 上場来高値DB（ローカルのみ・Cloud では high_history_snapshot.json から復元）
high_history.db
# high_history_snapshot.json は GitHub 永続化のため追跡対象

# 四季報「厳選注目株」の銘柄リスト（有料プラン限定コンテンツ）
# 購読者本人の手元でのみ使う。公開リポジトリに置くと有料記事の
# 「どの記事でどの銘柄が推奨されたか」が誰でも見られる状態になるため追跡しない。
shikiho_selection.csv
# memo_save.json と fundamental_list.csv は GitHub 永続化のため追跡対象

# ログ・一時ファイル
logs/
streamlit_log.txt
*.log
resp.json
cacert.pem

# キャッシュ
.cache/
*.pkl

# 環境設定
.env
.venv/
venv/
env/

# エディタ
.vscode/
.idea/
*.swp
*.swo

# Windows固有
Thumbs.db
Desktop.ini
daily_earnings_report.bat
daily_earnings_report.ps1

# MacOS
.DS_Store

# 再構築済みファイル（バックアップ）
app_reconstructed.py
main.py

# 四季報分析の計算結果CSV（銘柄リストと同じ理由でローカル限定）
shikiho_result.csv

# 某哲也ブログ由来の銘柄リスト（boutetsuya_stocks.csv）は、
# 高配当株PFタブで常時表示するため追跡対象にしている。
# 出典URLと記事ごとのリンクを画面に明記すること。
